function haba(x: isInt, y: isReal): isNull {
    println("int, real")
}

function haba(x: isReal, y: isInt): isNull {
    println("real, int")
}

// this should work
haba(2, 3.1);

// this should fail at compile time
// with ambiguity error
haba(1, 1);