interface haba;

function haba(x: isInt, y: isReal): isReal {
    x + y
}

function haba(x: isReal, y: isInt): isReal {
    x + y
}

assert(haba(2, 3.1) == 5.1);
assertError(() => haba(1, 1), "ambiguity error");