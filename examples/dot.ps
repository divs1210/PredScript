interface inc;
interface quadratic;

function inc(x: isReal): isReal {
    x + 1
}

// x^2 + x + 1
function quadratic(x: isReal): isReal {
    x
    .pow($this, 2)
    .($this + x)
    .inc
}

println(quadratic(2));