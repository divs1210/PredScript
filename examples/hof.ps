interface makeAdder;

function makeAdder(x: isInt): isFn {
    interface adder;

    function adder(y: isInt): isInt {
        x + y
    }

    adder
}

let add5: isFn = makeAdder(5);
println(add5(6));

println(makeAdder(2)(3));