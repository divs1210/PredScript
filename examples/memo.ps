interface f;

memoized function f(x: isInt): isNull {
    // TODO: mutate a var instead of printing,
    // and assert the value
    println("f called with: " + x);
}

f(5);
f(5); // this call will be skipped
f(6);