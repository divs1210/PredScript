let m: isMap = Map(
    0,       1,
    "a",     2,
    List(0), '_'
);

println(m);
println(size(m));
println(isEmpty(m));
println("m[List(0)]: " + m[List(0)]);
println("m[0] = 10: " + set(m, 0, 10));